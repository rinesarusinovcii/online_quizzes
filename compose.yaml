services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=online_quizzes'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432:5432'
